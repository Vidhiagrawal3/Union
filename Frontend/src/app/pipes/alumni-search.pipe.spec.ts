import { AlumniSearchPipe } from './alumni-search.pipe';

describe('AlumniSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new AlumniSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
